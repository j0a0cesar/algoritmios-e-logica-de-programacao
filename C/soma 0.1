
#include <stdio.h>

 int main () 
{ 
 int x, y, soma; 
 
   printf("\n Digite o primeiro valor a ser somado: ");
   scanf("%d", &x);
   printf("\n Digite o segundo valor a ser somado: ");
   scanf("%d", &y);
 
 soma= x + y; 

  printf("A soma é: d%", soma);

return 0
}
