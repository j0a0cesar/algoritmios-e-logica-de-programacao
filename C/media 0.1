
#include <stdio.h>

  int main() 
{
  float not1, not2, not3, not4, med;
   printf("Digite o valor da sua primeira nota: ");
   scanf("%f", &not1);
   printf("Digite o valor da sua segunda nota: ");
   scanf("%f", &not2);
   printf("Digite o valor da sua terceira nota: ");
   scanf("%f",&not3);
   printf("Digite o valor da sua quarta nota: ");
   scanf("%f", &not4);
   med = (not1+not2+not3+not4)/4 ;
   printf(" \n O valor da media entre as notas %0.2f, %0.2f, %0.2f, %0.2f e igual a %0.2f ", not1, not2, not3, not4, med);

 return 0;
}
